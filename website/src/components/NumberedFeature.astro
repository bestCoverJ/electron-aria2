---
interface Props {
  number: string;
  title: string;
  description: string;
}

const { number, title, description } = Astro.props;
---

<article class="number-card">
  <span class="number">{number}</span>
  <h3>{title}</h3>
  <p>{description}</p>
</article>
